# Backend

This folder contains the Python backend for the Claim Reimbursement Application.

- Source code is in `src/`
- Tests are in `tests/`
- Configuration files: `requirements.txt`, `.env.example`
