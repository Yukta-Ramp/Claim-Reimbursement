# Frontend

This folder contains the React frontend for the Claim Reimbursement Application.

- Source code is in `src/`
- Static assets are in `public/`
- Configuration files: `package.json`, `vite.config.ts`
